spring.application.name=gemes

server.port=8080

spring.datasource.url= # 空にする
spring.datasource.username= # 空にする
spring.datasource.password= # 空にする
spring.datasource.driver-class-name= # 空にする

# 以下の設定でデータソースの自動設定を無効化する
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
